name: efootball_epic_radar
description: "تطبيق حقيقي لتتبع كروت الإيبك في إي فوتبول"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  # هذه المكتبة الحقيقية المسؤولية عن جلب البيانات اللحظية من سيرفر eFootball DB
  http: ^1.2.0 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
